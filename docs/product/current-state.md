# YourTJ-Hub 当前状态

> Doc type: status matrix
>
> Status: Active
>
> Owner: Platform maintainers
>
> Last verified: 2026-09-05

## What works

- **Feature coverage**: Markdown topics/replies with client-side Mermaid diagram
  rendering, categories, notifications, direct messages, drafts,
  RBAC moderation, admin panel, theme workbench, i18n (en/zh/ja/de), GitHub OAuth + optional Google OAuth + built-in
  OIDC Provider (PKCE), TOTP 2FA + recovery codes, session management (jti + user_sessions, per-session revoke),
  scheduled SQLite backup, slow-SQL logging, aggregate search (topics/users/categories with scope
  tabs, pinyin/initials), sensitive-word moderation with review queue, terms-of-service page, data
  import/export, pluggable file storage (SQLite BLOB / S3-compatible), admin-managed bot personas
  (Agents) with unique bearer tokens and webhook endpoints, and configurable AI-readable public-content
  exports (`/llms.txt`, `/llms-full.txt`, `/p/posts/{id}.md`).
- **Wiki 分站（GitHub 唯一真实源）**: wiki 内容由公开 GitHub 仓库 `YourTongji/YourTJ-Wiki` 维护
  （PR 协作编辑、审核、历史、贡献者），论坛为只读投影——SSR `/wiki` + `/wiki/*path`
  （wiki.home/wiki.detail）与公开 API（tree/namespaces/home）；同步触发 = 每日定时（默认
  03:00，可配）+ 管理端手动 + GitHub webhook（push 事件，PR merge 后即时）；站内无写路径
  （编辑/历史按钮外链 GitHub）；topics.topic_type 隔离论坛 feed 与搜索；`wiki_sync_runs`
  记录每次同步（running/success/failed + 变更计数）。仓库目录逐级投影为可折叠导航：目录
  不要求 `index.md`，`index.md` 是该目录下的普通可点击页面，同级节点按 `order` 排序。
- **Built-in OIDC Provider**: the forum issues standard OIDC tokens (authorization code + PKCE S256,
  RS256 id_token, opaque access tokens) for first-party clients; `sub` is always the numeric users.id.
- Monorepo structure (apps/packages/services/deploy/docs) + CI (server/web/contract workflows).

## Current key gaps

| Domain | Status | Note |
|---|---|---|
| Forum itself | `Partial` | Upstream features complete and runnable; `make build` single binary verified locally (2026-08-06: go vet/test, pnpm typecheck/build, smoke all green); 通知链接按楼层号稳定跳转 `/p/post/{topicId}/{postNo}`，历史缺楼层号通知保留 `#post-{id}` 锚点回退（issue #368）；**内容类型体系已收敛**（issue #381：contentType 1=提问、2=瞬间（原"想法"更名）、3=文章；兼容值 0=普通帖子在读/写路径统一归一为 3，迁移 v28 幂等回填存量 0→3，列表/详情/编辑全量归一）；**图集已落地**（写接口支持显式 `images`，`TopicDetailPayload` 增 `firstImageUrl`/`images`；服务端校验图集文件归属当前上传者并设数量上限，防止引用他人文件使其删除/隐私清除后仍被托管）；**回复策略放开**（瞬间/文章允许楼层回复；有回复的话题仍禁止切换内容类型）；**发布入口升级**（navbar/移动 FAB 内容类型选择菜单 + 快捷发布弹窗：瞬间多图压缩上传/拖拽排序/自动取标题，提问/文章回落完整表单，发布页 `?type=`/`?contentType=` 预选）；**帖子详情页按类型差异化**（瞬间短内容楼层流、文章博客式排版、类型徽标；多图 TopicImageGallery 画廊 + MarkdownImageViewer 重构修复分享长图渲染白屏）；**路由别名**（`/topics/:id(/:postNo)` 指向详情 SSR，契约路由快照同步）；**移动端 UX**（PostComposer 重构、课评编辑器移动工具栏 slim 化与面板避让、Wiki 移动端搜索入口、LogoLoader 加载动画）；**友链页 hover card**（`/links` 卡片描述 truncate 截断时，hover/键盘 focus 经 `gf-tooltip` 展示完整描述，主题变量深浅色自适应，无 JS noscript 回退本就完整展示，issue #471）（2026-09-05） |
| Web Push 系统推送 | `Current` | 站内通知的第二投递通道（issue #444；#449 为页面内浏览器通知）：**页面完全关闭也能收到系统推送**——评论/回复（含关注内容新回复）/关注/点赞/徽章/wiki 更新在通知落库后由 worker 异步外投（Firefox/Safari 直连生效；Chrome 走 FCM，境内端点不可达为已知边界；iOS 需 Safari 且「添加到主屏幕」后生效）；推送标题/正文/深链按订阅语言（zh/en/ja/de）服务端渲染，楼层号稳定优先、wiki 用 ProfileURL、其余回落通知中心；订阅开关在设置页管理（浏览器授权/退订同步后端）；需实例配置 VAPID 密钥（`[webpush]` 段，`webpush-keys` 命令生成）后启用，dev 实例不配置（通道关闭，快照同步的订阅/任务行不外发） |
| 浏览器级通知（页面内） | `Current` | issue #444 第一通道（Web Push 为页面关闭也能收到的第二通道）：纯前端 Web Notification API，设置页 privacy 独立开关申请权限 + localStorage 偏好；复用 unread-status 30s 轮询（契约含单调信号 `latestUnreadId` = 最新未读通知 id，新通知前移、读旧不影响），页面后台时按「已见游标推进」触发——**未读持续期间新到事件不再漏报**（同 id 不重弹、页面可见只推进不弹、清零重置游标；旧响应无 id 时回落「无未读→有未读」翻转语义）；按 latestNotificationType 分文案（comment/post_reply/topic_post/follow/like/badge/wiki_updated，zh/en/ja/de）；点击经 SPA 导航桥 router.push 跳转通知中心（注册前极端时序回退整页跳转）；多标签页 BroadcastChannel + localStorage 去重；**与 Web Push 跨通道去重**：SW 弹出后向共享频道广播 {at}，本通道在 60s 去重窗口内让位；SW 不可达（未订阅/实例未配 VAPID/境内 Chrome FCM 不通）时不广播，本通道自动兜底；权限被拒/浏览器不支持时优雅降级为红点 + 文档标题（2026-09-05） |
| Wiki 分站 | `Current` | GitHub 仓库为唯一真实源（`YourTongji/YourTJ-Wiki`，公开，PR 协作编辑）：SSR `/wiki` + `/wiki/*path`（wiki.home/wiki.detail）、公开 API（tree/namespaces/home）、管理端 `/admin/wiki`（PageManager：命名空间只读列表 + 只读页面树 + GitHub 同步面板：状态/手动同步/运行记录 + webhook 验签密钥管理（securestore 加密；管理端清除后即使旧明文配置存在也保持禁用））；同步触发 = 启动时异步 + 每日定时（默认 03:00，可配）+ 手动 + webhook（HMAC-SHA256 验签，push 事件）；命名空间 = 仓库顶层目录名（支持中文等 Unicode，目录消失自动删除），**URL 语义 = 仓库目录名**：`wiki_pages.path` 首段与 `namespace` 列即仓库顶层目录名（显示名），`source_path` 恒存仓库真实相对路径（GitHub 编辑/历史外链用）；页面 = 目录内 `.md`（frontmatter title/order/description；`index.md` 携带命名空间描述/排序）；路径校验（支持中文等 Unicode，段内拒绝空白/控制/保留字符与点开头）失败的正式页面 → 同步 fail-fast 报错（run 标记 failed，绝不静默跳过），仓库根级 README/CONTRIBUTING 等元文件显式排除；**Git 重命名/移动文件（内容不变）按 `content_hash` 唯一匹配收养**——迁移 path/parent_id、恢复软删、复用原 topic，回复/点赞/收藏/订阅/watcher 通知跟随新路径（issue #288；同 hash 多候选或内容同时变化时 fail-safe 新建+软删旧页）；站内无写/回滚/diff/编辑者/命名空间 CRUD API（编辑/历史/贡献者走 GitHub）；topics.topic_type 隔离 feed/搜索；`wiki_sync_runs` 记录同步（running/success/failed + 变更计数；崩溃/重启遗留的 running 行自动回收为 failed，管理端手动同步 accepted 后轮询至终态并刷新投影，issue #290）；OpenAPI wiki 域覆盖公开读 + 管理同步端点（TS 类型 + 手写 Dart mirror） |
| Database | `Current` | PostgreSQL is the default deployment database (`deploy/config.toml.example`); SQLite stays the local development/test default; file db stays SQLite; MySQL is not supported; data migration from SQLite→PG is manual |
| Search | `Partial` | Aggregate search landed (issue #22): one search box covers topics, users and categories with grouped sections and scope tabs; pinyin/initials matching for users and categories; index sync via topic/user/category events + migration v13 rebuild; per-scope partial degradation; unavailable-state UI fallback |
| Course reviews (课评) | `Partial` | Cross-dialect course catalog schema (course/alias/term/offering/instructor/offering-instructor/import-run/source-ref), course identity is the **(code, teacher) composite card** (issue #326): each upstream course row is one card, unique on (primary_code, teacher_id), reviews attributed row-exactly via reviews.course_id; offline `course-import` CLI with manifest checksum + dry-run + quarantine + idempotent retry (`catalog` + `reviews` subcommands; reviews gated on manifest `rights_approval_ref`), PG read service with keyword/teacher/term/campus filters (each accepts repeated or comma-separated values, OR-joined), SSR `/courses` + `/courses/:courseId`（课程目录/课程管理/详情课评列表为滚动到底自动加载，issue #246；`?offeringId=` 聚焦单教学班评价，offering 校验 + 写评预选 + 统计卡课程级口径；课程目录筛选支持院系/学期/校区/教师多值并集；登录用户可收藏课程（`POST /api/forum/courses/bookmark`，幂等 action 1/2）并在目录表格与详情页显示收藏态） and JSON `GET /api/forum/courses(/{courseId})` (OpenAPI-covered, route contract tests green); Meilisearch `courses` scope with persistent transaction-bound outbox worker + `rebuild-course-search`/`reconcile-course-search`; native review write/update/delete/helpful/report with anonymous zero-leak DTO, CourseManager moderation (hide/show + report queue) and Admin-only audited identity reveal, `rebuild-course-stats` CLI; **PK 排课器 14 端点** (`/api/pk/*`，统一 `{code,msg,data}` 信封，OpenAPI 契约 + route contract tests green，含 teacher_timeslots 懒构建/降级；P1 calendars 返回可选学期起止日期 startDate/endDate（config `[pk.semester_dates]` 维护，course-pk-sync 按 calendar_id_i18n 写入 pk_calendar，未配置为 null）；P13 course-review-brief 返回课程级 + 教学班级（classes）课评摘要，支持可选 calendarId 学期限定，班号去点归一化匹配 offering.class_code，隐藏课程/隐藏 offering 不出现在公开响应，无教师关联时 teachers 序列化为空数组；学期标记支持中英文格式归一化（calendar_id_i18n "2025-2026学年第2学期" ↔ course_term.code "2025-2026-2"），响应含课程级 ratingDistribution 供右侧课评面板复用评分仪表卡)；一系统在线同步管线 `course-pk-sync <学期>` (issue #186): PK 域表（pk_calendar/pk_language/pk_course_nature(+_by_calendar)/pk_assessment/pk_campus/pk_faculty/pk_major/pk_major_course/pk_course_detail/pk_teacher/pk_teacher_timeslot/pk_fetch_log），分页抓取 200/页 + 500 行/批事务写入 + `PkFetchLog` 断点续跑（崩溃从失败批次续）+ `teacher_timeslots` 重建 + 可选 `--materialize` 物化课程目录；凭证经 `--onesystem-cookie` / `ONESYSTEM_COOKIE` env / 管理端设置（securestore 加密落库，`onesystem-settings` admin API）；Vue3 排课器 (`/schedule`) `Partial`（学期/年级/专业选择（侧栏配置区可折叠为单行摘要，`goose:scheduleConfigCollapsed` 记忆折叠状态）；专业字段复用原选择框直接输入关键词筛选，校区和开课学院的长选项列表在下拉内支持本地关键词筛选（issue #329）；桌面左右两栏同高（左栏方案/学期专业/统计/已选列表内部滚动，右栏课表为主视觉定高；点击课程弹「选择教学班」浮窗，移动端双 tab 课表/选课（APG tabs 方向键导航）；多方案「我的方案」条（新增/切换/删除/清空，各方案独立持有课程与自定义占位，`pk.plans`+`pk.activePlanId`持久化并从 v1 旧键迁移，「同步最新」按课号保留各方案排课状态）；容忍式冲突（加课不再强替弹窗，总是入表：课表 ⚠ 角标 + 列表红标 + flash 提示，统计卡显示冲突门数）；周次视图（全部周次/第 1-16 周过滤，同格多课竖向堆叠显示课名+周次——单双周同位共存（周次无交集）不判冲突且两门都显示，「当前周次」定位依赖 P1 startDate，未配置时禁用；周次下拉「全部周次」用哨兵 value 规避 reka-ui SelectItem 空串禁令）；11/12 节课制自适应网格（`calendarId>=120` 为 11 节新课制），同教学班多段安排（同天同班号）智能合并为单卡，行高按单双周堆叠需求自动扩展；自定义「有事」占位（星期/节次/周次，参与冲突提示）；统计卡（门数/总学分/总学时/冲突数）；已选列表富卡片与课程名/课号搜索；必修/选修/搜索/时间段加课、退课/清除（变更立即持久化 localStorage）、课表导出 CSV/XLS/PNG（html-to-image；PNG 走独立高清海报浮层，img-fx 渐显动效完成后才放行导出/复制，冲突课表导出前有冲突汇总确认）、课表空白格点击选课、节次起止时间与上午/下午/晚上分组（12 节制默认锚点：第 3 节 10:00/第 5 节 13:30/第 7 节 15:30/第 10 节 18:30，管理面板「设置 → 节次作息」热配置——`GET/POST /api/admin/(save-)schedule-settings`，page_config 存储 + SSR 直读 DB（保存即生效，无 5s 缓存窗口）+ 管理端保存后清缓存 + HTML no-store（防 bfcache 陈旧）+ 前台 pageshow/可见性恢复时静默重拉同步作息（节流 5s、失败静默）+ OpenAPI 契约覆盖，SSR props `sectionTimes` 注入、未配置走前端默认表）、课程/教学班课评入口与聚焦跳转，外部选课/排课工具入口（课表工具条气泡）；「选择教学班」浮窗桌面双栏——右侧浮动课评面板（复用详情页 RatingSummaryCard 评分仪表 + 班级课评列表 + 完整课评入口，移动端单栏堆叠），P11 过期提示 + P12 全方案并集增量同步）；30-day deletion isolation window (B3 privacy, issue #175): `course-review-cleanup` job (daily cron enqueue + background worker + manual CLI) anonymizes expired deleted rows — content cleared, author released via NULL, unique slot freed, row kept for audit — with migration v17 backfilling legacy `deleted_at` anchors; mobile course UI remains `Planned`; in-course review-body search is recorded as **Won't** (issue #184: review body stays out of the `courses` index and no separate `course_reviews` index will be added, per PRD §5.1 B5 / RICE 0.35)；AI 课程总结（issue #181）provider 参数（endpoint/model/apiKey/temperature/maxTokens）可在管理面板「AI 总结」设置中热配置（apiKey securestore AES-256-GCM 加密落库且不回显，留空保留已存密钥；管理端配置了 endpoint 或 model 时优先于 config.toml `[ai_summary]`，均未配置时回退）；总结结果与「评价不足已评估」标记均落库 `course_ai_summary`（status=generated/insufficient，评价变更自动失效重评；≥1 条可见有正文评价即可生成，insufficient 标记命中时以当前评价数为准自愈重判，评价增加后自动恢复生成），`?check=true` 只读预检（cached/insufficient_data/none/disabled，不生成不消耗限流）供前端挂载判断：已有总结自动展开、右上角手动刷新（`?refresh=true` 强制重评）；生成限流后置（数据不足不消耗名额，LLM 失败返还单课名额，避免无谓 429）；管理端 `POST /api/admin/ai-summary-models` 自动拉取模型列表，不支持 /models 的服务可手动输入 model 兜底 |
| Course aggregation & lineage (课评聚合/2026 课程沿革) | `Current` | 课程卡 `review_scope` 三档（teacher 默认 / team 团队聚合 / course 课程级）+ `team_key` 教学团队键（管理端课程编辑维护）；team 档详情评分/分布/教师名单按团队读时聚合（「教学团队 · 张三、李四等 N 位教师」）；`course_relations` 沿革表（EQUIVALENT/RENAMED_FROM 可合并，SPLIT_FROM/MERGED_FROM/RELATED 仅展示；rule/manual 来源 + 证据快照）+ 确定性规则引擎 `courseservice/lineage`（教学班级级 R1-R5 dry-run `course-lineage-scan`；卡级 `course-lineage-seed` 装配可见课程卡产 course.id 候选，默认 dry-run，--write/--write-family 幂等落 pending）+ 管理端「课程沿革」审核面板（候选列表按状态/关系类型双过滤，支持批准/忽略/撤回处理决定（approved/ignored → pending，merged 须走撤销合并）/手动建关系/确认等价合并/撤销合并，全部审计入 `opt_record`）；合并物理迁移 offering（评价/教师关联随行零丢失）+ 别名（冲突跳过并记录）、旧卡隐藏、`evidence_json` 快照可撤销；详情页原名标注（legacyNames）+ 沿革区块（lineage，方向 to/from）+ team/course 徽标；P13 课评摘要支持 `teachingClassId` 教学班直查（找不到回退 courseCode/teacherName 猜测）；offering 权威写入源 = 一系统物化（`course-pk-sync --materialize` 班粒度幂等 upsert，teaching_class_id+term 唯一索引，落库 teacher_code，不写 status 不复活隐藏班），历史数据包导入兼容从属（复用已有 offering 行）；契约新增 7 操作（course-relation-list/approve/ignore/create/reset、course-merge/-undo）+ fixtures + Go 合约测试；SSR payload / OpenAPI TS / Dart 镜像同步 |
| Auth | `Partial` | Password + TOTP 2FA + GitHub OAuth + optional Google OAuth + built-in OIDC Provider (authorization code/PKCE S256, RS256 id_token, opaque access tokens, numeric sub) implemented; per-client `redirect_uris_globs` doublestar patterns as fail-closed fallback to exact redirect matching for third-party OIDC clients with dynamic callback query/path segments; mobile exchange keeps the forum-JWT contract; bot personas are rejected by all human-session and OIDC paths |
| Agents (bot personas) | `Partial` | Admin-managed lifecycle and six-operation Agent forum API are `Current`: users row with explicit actor type (human/bot), one token per agent stored only as hash + non-secret prefix (`agt_`), enable/rotate, and disable that revokes the credential (re-enabling requires rotation), dedicated bearer authentication, published topic listing, topic/post writes, post windows and aggregate search; bot rows are rejected by all human-auth paths; mention parsing and webhook wakeups remain `Planned` |
| Contract | `Partial` | OpenAPI 3.1 covers **every non-excluded `/api` JSON route** (auth/account/session/TOTP, forum writing/interactions/search/statistics, user account + content lifecycle, notifications/chat, moderator workbench, full `/api/admin/*` console, Agent public API, courses/reviews, PK scheduler, Wiki GitHub-SSoT domain); the generated `packages/api-contract/coverage-matrix.md` is the authoritative route list and CI rejects any route that is neither contracted nor listed; Redocly lint/bundle, fixture validation, generated TypeScript no-diff checks, committed fixtures and real Gin route tests are in CI; mobile Dart mirrors stay hand-maintained under fixture deserialization checks; remaining gaps: OIDC Provider standard endpoints (separate track), AI-readable text surfaces, automated Dart generation (`Planned`) |
| Mobile | `Partial` | Flutter client (`apps/mobile`, melos: core/auth/ui_kit/forum_app): persistent four-destination shell (home/search/messages/profile) with per-branch state and a central global publish action; explicit 44dp back actions on pushed pages and long-page scroll-to-top; Web-aligned list/card topic feeds; redesigned topic detail and profile; global Markdown publish editor (narrow-screen edit/preview switch, wide two-column editor+preview, formatting/image toolbar, drafts and edit prefill); reply composer with image action; structured skeleton loading; unified settings/login/notifications/drafts; OIDC exchange login with dot-grid auth cards; repo-owned `ui_kit` Gf* components over pinned TDesign v1 alpha; CI `ci-mobile`; not store-deployed — push notifications, custom theme sync, and ja/it locales remain gaps |
| Points | `Partial` | Forum-local ledger mechanics are `Current`: topic/reply rewards are source-key idempotent, reply deletion reverses its reward atomically, and migration v14 reconstructs missing legacy balance rows. End-to-end reward delivery remains `Partial` because the event bus is in-memory and has no reconciliation after process loss. Cross-platform settlement through services/credit remains `Planned`; services/credit is still a deployment placeholder. |
| Branding | `Partial` | Default UI copy, activation template, locales and admin brand settings rebranded to YourTJHub (2026-08-07); default brand assets refreshed 2026-09-05 from designer SVG exports (cat-circle YTJ icon + YourTJ社区 wordmark): `resource/static/pic/brand-default.{png,webp}` (light variant, black wordmark) + `brand-default-dark.webp` (dark variant, white wordmark) switched by site theme in AppShell/LoginPage/ResetPasswordPage; icon set (`icon.webp` 80, `icon_300.webp` 300, `icons/favicon-32.png`, `icons/icon-192.png`, `icons/apple-touch-icon.png` white-backed) cropped from the dark-variant icon; admin `brandType=image` (safeUrl-validated) overrides with uploaded `/file/img/…` (color-agnostic, no theme switch) on AppShell/LoginPage/ResetPasswordPage; `brandType=default` ignores residual `brandImage` (legacy dirty configs, 2026-09-05) and always uses the themed wordmark; mobile `assets/images/brand-default.png` still carries the previous wordmark (remaining gap: Flutter golden regeneration); CLI name (`gooseforum`) kept for upstream merge; Go module path moved to monorepo (github.com/YourTongji/YourTJ-Hub/apps/gooseforum, issue #127) |
| Structural governance | `Partial` | Upstream giant controllers (payload.go 72KB etc.) not split; architecture decisions in note |
| Storage (files) | `Current` | Pluggable storage: local SQLite BLOB default + S3-compatible object storage (MinIO/COS/OSS/R2), admin panel config + connection test, cursor-driven BLOB→object migration task + `migrate-files` CLI (2026-08-06); S3 provider 下帖子图片走浏览器直传（`/file/img-upload/init` 预签名 POST 策略 → 直传 bucket → `/file/img-upload/complete` 服务端校验归属/大小/MIME/解码图片头后发布，未完成对象 2 小时清理），本地提供方保持服务端代理 multipart 上传（issue #366） |
| Moderation policy | `Current` | Reserved/banned usernames, sensitive-word block or review (ProcessStatus=2 pending queue with admin approve/reject), banned username auto-freezes existing accounts, moderation audit logs. 策略引擎升级（2026-09-02）：自建 AC/归一化匹配引擎（大小写/NFKC 全半角/零宽折叠，名单另含 ASCII leetspeak 全等），reserved/banned 名单同规则覆盖 username 注册/改名、OAuth 建号（命中退避 `_n`）、Agent 建号（拒绝）与昵称（整串全等）；敏感词归一化子串扫描覆盖话题/回复/私信/课评（block，`course.review.sensitiveBlocked`）与个人资料 bio/signature/website 自由文本；内置默认词库（reserved 73 / sensitive 37，来源 fwwdn/sensitive-stop-words Apache-2.0，banned 恒空防误冻结），存量空数组由 v27 数据迁移补齐；管理端支持粘贴批量导入（预览/去重/冻结警示） |
| Terms of service | `Current` | Editable ToS (markdown) in admin, rendered at `/terms`, registration page links and agreement checkbox (2026-08-06) |
| Data import/export | `Current` | Admin panel JSON/CSV export (users/topics/posts, background task + download; derived topic_category_index / topic_user_stat tables export when selected) and JSON import with per-row validation report and idempotent skip; topic invariants (post_seq, first/last post pointers, counts, posters) preserved and rebuilt on import (2026-08-12); export files retained 7 days (2026-08-06) |
| Abuse protection | `Current` | Per-action rate limiting (memory fixed-window, IP+user) on register/login/forgot-password/reset-password/oidc.authorize/oidc.token/email.change/password.change/totp.setup/totp.enable/totp.disable/topic.write/topic.status/post.create/post.delete/message.send/upload/interact/llms.index/llms.full/llms.topic/mcp.auth/course.catalog/course.bookmark/course.review.write/course.review.helpful/course.review.dislike/course.review.report/course.review.reveal/course.review.moderate/course.summary/course.summary.check（AI 总结 check 预检独立配额）; 429 + Retry-After; captcha switch + new-user post threshold + honeypot + submit-timing detection; all limits hot-tunable in admin settings; 每用户每日新主题上限可配置（管理后台发布设置 `maxDailyTopicsPerUser`，默认 10、0=不限额，达到上限即拒绝；按服务器本地时区计当日新主题，含软删/待审/草稿与 wiki 页面主题；仅约束新建，编辑/回复不受影响；负值在保存/读取路径归一为 0，issue #369） |
| HTTP security headers | `Current` | Unified security headers via `app/http/middleware/securityHeaders.go` (registered in `bridge.go`, issue #407): every response carries `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: camera=() microphone=() geolocation=()`; HTML page routes (viewRoute/`/activate`, incl. HTML 404/500 pages) additionally carry a page `Content-Security-Policy` (`default-src 'self'`, `script-src 'self'`, `object-src 'none'`, `frame-src 'none'`, `frame-ancestors 'none'`, `base-uri 'self'`, `form-action 'self'`, `style-src 'self' 'unsafe-inline' https://fonts.googleapis.cn`, `font-src 'self' https://fonts.gstatic.cn`, `img-src 'self' http: https: data: blob:`, `connect-src 'self' http: https:` + `ws: wss:` in non-production for Vite HMR) — clickjacking is blocked by both `frame-ancestors 'none'` and `X-Frame-Options: DENY`; `X-Powered-By` removed; /api /file /mcp /assets /static /health surfaces keep universal headers but no CSP (browser ignores CSP on non-document responses; OIDC authorize UI is under /api/oauth and intentionally unconstrained); route-level tests (`app/http/routes/security_headers_test.go`) and middleware unit tests cover HTML/API/error pages and the dev/prod CSP difference |
| AI-readable content | `Current` | Admin posting settings independently gate the llms.txt index, full-text export, and per-topic Markdown; exports include only published topics with normal first posts and normal, non-deleted replies; generated content is cached for 10 seconds and invalidated by topic/reply/category events, direct clear on moderation/reply-edit/topic-category/unpublish paths, or relevant setting changes; full export is hard-capped (5000 topics / 8 MiB / 30 s) and truncated with a marker |
| MCP server | `Partial` | Official MCP server (issue #93) ships in the single binary: `/mcp` streamable HTTP endpoint + `mcp-stdio` subcommand, exposing the six-operation Agent forum API as curated handwritten tools (me / list_topics / get_posts / search always; create_topic / create_post only when the MCP write setting is enabled). Both the endpoint (`mcp.enabled`) and write tools (`mcp.writes`) are managed from the admin panel (Settings → MCP server), stored in DB via `page_config` and applied hot (5s cache) without a restart; both default to off. When `mcp.enabled` is off, `/mcp` answers 404 and exposes no MCP surface. Auth reuses the `agt_` bearer token via `agentservice.ResolveByToken`, with unauthenticated floods bounded by the shared `mcp.auth` per-IP rate limit; write tools share the existing topic.write / post.create rate limits (IP + bot userId, SkipAdmin exemption), and `mcp-stdio --writes` can override the write setting per local session. `/mcp` requests lift the 10s server write timeout (GET SSE streams stay unlimited, bounded by the 15m session timeout; POSTs get a finite 60s write deadline so a client that stops reading cannot pin a session). Public-facing OAuth 2.1 + RFC 9728 resource metadata, SSE fallback for domestic clients, and webhook/mention wakeups remain `Planned` |

## Correctness first

Before expanding features, close these baselines (avoid building on a wrong foundation):

1. Decide MFA policy for built-in OIDC / GitHub OAuth login paths (forum TOTP reuse is a `Decision needed`).
2. Expand OpenAPI and generated-client coverage before broad API rework, so uncovered routes do not
   become a new source of contract drift.
