# Placeholder: once the contract pipeline exists, openapi.yaml is generated from the
# Go structs in apps/gooseforum.
# Generation: cd apps/gooseforum && swag init --output ../../packages/api-contract
# Consumers: gen-ts.sh → apps/gooseforum/resource/packages/client
#            gen-dart.sh → apps/mobile/packages/core/lib/src/gen/*.dart
openapi: 3.1.0
info:
  title: yourtj-hub API
  version: 0.1.0
paths: {}
