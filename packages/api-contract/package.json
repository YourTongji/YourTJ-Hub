{
  "name": "@yourtj/api-contract",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "lint": "redocly lint openapi.yaml",
    "bundle": "redocly bundle openapi.yaml --output .tmp/openapi.bundle.yaml",
    "bundle:json": "redocly bundle openapi.yaml --output .tmp/openapi.bundle.json",
    "generate:ts": "openapi-typescript openapi.yaml -o ../../apps/gooseforum/resource/packages/client/src/gen/openapi.ts",
    "check:gen": "node scripts/check-ts-gen.mjs",
    "check:coverage": "node scripts/check-route-coverage.mjs",
    "check:fixtures": "node scripts/validate-fixtures.mjs",
    "check": "pnpm run lint && pnpm run bundle && pnpm run bundle:json && pnpm run check:fixtures && pnpm run generate:ts && pnpm run check:gen && pnpm run check:coverage"
  },
  "devDependencies": {
    "@redocly/ajv": "8.11.2",
    "@redocly/cli": "2.45.0",
    "js-yaml": "4.3.1",
    "openapi-typescript": "7.13.0"
  }
}
