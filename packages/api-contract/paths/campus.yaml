{
  "status": {
    "get": {
      "tags": [
        "Campus"
      ],
      "summary": "campusStatus",
      "operationId": "campusStatus",
      "security": [
        {
          "bearerAuth": []
        },
        {
          "accessTokenCookie": []
        }
      ],
      "description": "Private Tongji connection. One forum account and one official identity are mutually unique. Tokens are never returned. Authorization confirmation is session-bound and one-use. Refresh expiry reserves the binding.",
      "responses": {
        "200": {
          "description": "Successful private response; Cache-Control: private, no-store.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CampusStatusResponse"
              },
              "examples": {
                "success": {
                  "externalValue": "../fixtures/campus-status-success.json"
                }
              }
            }
          }
        },
        "default": {
          "description": "Session, CSRF, writable-account, expired authorization, identity conflict or upstream failure.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiFailure"
              }
            }
          }
        },
        "401": {
          "description": "Missing, invalid or revoked forum session.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiFailure"
              }
            }
          }
        }
      }
    }
  },
  "start": {
    "post": {
      "tags": [
        "Campus"
      ],
      "summary": "campusStart",
      "operationId": "campusStart",
      "security": [
        {
          "bearerAuth": []
        },
        {
          "accessTokenCookie": []
        }
      ],
      "description": "Private Tongji connection. One forum account and one official identity are mutually unique. Tokens are never returned. Authorization confirmation is session-bound and one-use. Refresh expiry reserves the binding.",
      "responses": {
        "200": {
          "description": "Successful private response; Cache-Control: private, no-store.",
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": [
                  "code",
                  "result"
                ],
                "properties": {
                  "code": {
                    "type": "integer",
                    "const": 0
                  },
                  "result": {
                    "type": "object",
                    "required": [
                      "url"
                    ],
                    "properties": {
                      "url": {
                        "type": "string",
                        "format": "uri"
                      }
                    },
                    "additionalProperties": false
                  }
                }
              }
            }
          }
        },
        "default": {
          "description": "Session, CSRF, writable-account, expired authorization, identity conflict or upstream failure.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiFailure"
              }
            }
          }
        },
        "401": {
          "description": "Missing, invalid or revoked forum session.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiFailure"
              }
            }
          }
        }
      },
      "requestBody": {
        "required": true,
        "content": {
          "application/json": {
            "schema": {
              "type": "object",
              "required": [
                "mode"
              ],
              "properties": {
                "mode": {
                  "type": "string",
                  "enum": [
                    "bind",
                    "replace",
                    "reauthorize"
                  ]
                }
              },
              "additionalProperties": false
            }
          }
        }
      }
    }
  },
  "confirm": {
    "post": {
      "tags": [
        "Campus"
      ],
      "summary": "campusConfirm",
      "operationId": "campusConfirm",
      "security": [
        {
          "bearerAuth": []
        },
        {
          "accessTokenCookie": []
        }
      ],
      "description": "Private Tongji connection. One forum account and one official identity are mutually unique. Tokens are never returned. Authorization confirmation is session-bound and one-use. Refresh expiry reserves the binding.",
      "responses": {
        "200": {
          "description": "Successful private response; Cache-Control: private, no-store.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiSuccess"
              }
            }
          }
        },
        "default": {
          "description": "Session, CSRF, writable-account, expired authorization, identity conflict or upstream failure.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiFailure"
              }
            }
          }
        },
        "401": {
          "description": "Missing, invalid or revoked forum session.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiFailure"
              }
            }
          }
        }
      }
    }
  },
  "unbind": {
    "post": {
      "tags": [
        "Campus"
      ],
      "summary": "campusUnbind",
      "operationId": "campusUnbind",
      "security": [
        {
          "bearerAuth": []
        },
        {
          "accessTokenCookie": []
        }
      ],
      "description": "Private Tongji connection. One forum account and one official identity are mutually unique. Tokens are never returned. Authorization confirmation is session-bound and one-use. Refresh expiry reserves the binding.",
      "responses": {
        "200": {
          "description": "Successful private response; Cache-Control: private, no-store.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiSuccess"
              }
            }
          }
        },
        "default": {
          "description": "Session, CSRF, writable-account, expired authorization, identity conflict or upstream failure.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiFailure"
              }
            }
          }
        },
        "401": {
          "description": "Missing, invalid or revoked forum session.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiFailure"
              }
            }
          }
        }
      },
      "requestBody": {
        "required": true,
        "content": {
          "application/json": {
            "schema": {
              "type": "object",
              "required": [
                "revision"
              ],
              "properties": {
                "revision": {
                  "type": "string",
                  "minLength": 1
                }
              },
              "additionalProperties": false
            }
          }
        }
      }
    }
  },
  "dataset": {
    "get": {
      "tags": [
        "Campus"
      ],
      "summary": "campusDataset",
      "operationId": "campusDataset",
      "security": [
        {
          "bearerAuth": []
        },
        {
          "accessTokenCookie": []
        }
      ],
      "description": "Private Tongji connection. One forum account and one official identity are mutually unique. Tokens are never returned. Authorization confirmation is session-bound and one-use. Refresh expiry reserves the binding. The profile dataset exposes only the authenticated student’s own name for a greeting. Academic datasets can be loaded on demand.",
      "responses": {
        "200": {
          "description": "Successful private response; Cache-Control: private, no-store.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CampusDatasetResponse"
              },
              "examples": {
                "success": {
                  "externalValue": "../fixtures/campus-dataset-success.json"
                }
              }
            }
          }
        },
        "default": {
          "description": "Session, CSRF, writable-account, expired authorization, identity conflict or upstream failure.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiFailure"
              }
            }
          }
        },
        "401": {
          "description": "Missing, invalid or revoked forum session.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiFailure"
              }
            }
          }
        }
      },
      "parameters": [
        {
          "in": "path",
          "name": "dataset",
          "required": true,
          "schema": {
            "type": "string",
            "enum": [
              "calendar",
              "timetable",
              "grades",
              "summary",
              "cet",
              "terms",
              "messages",
              "sports",
              "health",
              "arrangements",
              "profile"
            ]
          }
        }
      ]
    }
  },
  "callback": {
    "get": {
      "tags": [
        "Campus"
      ],
      "summary": "Finish Tongji authorization and return to identity confirmation",
      "operationId": "campusCallback",
      "security": [
        {
          "bearerAuth": []
        },
        {
          "accessTokenCookie": []
        }
      ],
      "description": "Validates session-bound state, S256, nonce, RS256 ID token and student identifier. Consumes the authorization attempt once. Every handled callback redirects to a clean campus URL with no-referrer and private, no-store, including missing/invalid/revoked sessions, non-writable accounts, rate limits, disabled configuration and upstream failures. Rejected guards never exchange the code.",
      "parameters": [
        {
          "in": "query",
          "name": "state",
          "schema": {
            "type": "string"
          }
        },
        {
          "in": "query",
          "name": "code",
          "schema": {
            "type": "string"
          }
        },
        {
          "in": "query",
          "name": "error",
          "schema": {
            "type": "string"
          }
        }
      ],
      "responses": {
        "303": {
          "description": "Redirect to /campus with success or failure status; no credential in Location.",
          "headers": {
            "Location": {
              "schema": {
                "type": "string"
              }
            },
            "Cache-Control": {
              "schema": {
                "type": "string",
                "enum": [
                  "private, no-store"
                ]
              }
            },
            "Referrer-Policy": {
              "schema": {
                "type": "string",
                "enum": [
                  "no-referrer"
                ]
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "CampusBinding": {
        "type": "object",
        "required": [
          "maskedId",
          "boundAt",
          "revision",
          "needsAuthorization"
        ],
        "properties": {
          "maskedId": {
            "type": "string"
          },
          "boundAt": {
            "type": "string",
            "format": "date-time",
            "description": "Time the current official identity was bound. Replacing the identity resets this time; renewing authorization for the same identity preserves it."
          },
          "revision": {
            "type": "string"
          },
          "needsAuthorization": {
            "type": "boolean"
          }
        },
        "additionalProperties": false
      },
      "CampusCandidate": {
        "type": "object",
        "required": [
          "maskedId",
          "mode",
          "expiresAt"
        ],
        "properties": {
          "maskedId": {
            "type": "string"
          },
          "mode": {
            "type": "string",
            "enum": [
              "bind",
              "replace",
              "reauthorize"
            ]
          },
          "expiresAt": {
            "type": "string",
            "format": "date-time"
          }
        },
        "additionalProperties": false
      },
      "CampusStatus": {
        "type": "object",
        "required": [
          "enabled",
          "binding",
          "candidate"
        ],
        "properties": {
          "enabled": {
            "type": "boolean"
          },
          "binding": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/CampusBinding"
              },
              {
                "type": "null"
              }
            ]
          },
          "candidate": {
            "oneOf": [
              {
                "$ref": "#/components/schemas/CampusCandidate"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "additionalProperties": false
      },
      "CampusMetric": {
        "type": "object",
        "required": [
          "label",
          "value",
          "unit"
        ],
        "properties": {
          "label": {
            "type": "string"
          },
          "value": {
            "type": "string"
          },
          "unit": {
            "type": "string"
          }
        },
        "additionalProperties": false
      },
      "CampusPoint": {
        "type": "object",
        "required": [
          "label",
          "value"
        ],
        "properties": {
          "label": {
            "type": "string"
          },
          "value": {
            "type": "number"
          }
        },
        "additionalProperties": false
      },
      "CampusEvent": {
        "type": "object",
        "required": [
          "name",
          "teacher",
          "room",
          "campus",
          "credits",
          "day",
          "start",
          "end",
          "weeks"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "teacher": {
            "type": "string"
          },
          "room": {
            "type": "string"
          },
          "campus": {
            "type": "string"
          },
          "credits": {
            "type": "string"
          },
          "day": {
            "type": "integer"
          },
          "start": {
            "type": "integer"
          },
          "end": {
            "type": "integer"
          },
          "weeks": {
            "type": "array",
            "items": {
              "type": "integer"
            }
          }
        },
        "additionalProperties": false
      },
      "CampusDataset": {
        "type": "object",
        "required": [
          "key",
          "status",
          "updatedAt",
          "metrics",
          "series",
          "events",
          "columns",
          "rows"
        ],
        "properties": {
          "key": {
            "type": "string",
            "enum": [
              "calendar",
              "timetable",
              "grades",
              "summary",
              "cet",
              "terms",
              "messages",
              "sports",
              "health",
              "arrangements",
              "profile"
            ]
          },
          "status": {
            "type": "string",
            "enum": [
              "ready",
              "empty",
              "unavailable"
            ]
          },
          "updatedAt": {
            "type": "string",
            "format": "date-time"
          },
          "metrics": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CampusMetric"
            }
          },
          "series": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CampusPoint"
            }
          },
          "events": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CampusEvent"
            }
          },
          "columns": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "rows": {
            "type": "array",
            "items": {
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          },
          "messages": {
            "type": "array",
            "description": "Only populated for messages; newest publication first. IDs are decimal strings.",
            "items": {
              "$ref": "#/components/schemas/CampusMessageSummary"
            }
          }
        },
        "additionalProperties": false
      },
      "CampusStatusResponse": {
        "allOf": [
          {
            "$ref": "../components/schemas.yaml#/ApiSuccess"
          },
          {
            "type": "object",
            "required": [
              "result"
            ],
            "properties": {
              "result": {
                "$ref": "#/components/schemas/CampusStatus"
              }
            }
          }
        ]
      },
      "CampusDatasetResponse": {
        "allOf": [
          {
            "$ref": "../components/schemas.yaml#/ApiSuccess"
          },
          {
            "type": "object",
            "required": [
              "result"
            ],
            "properties": {
              "result": {
                "$ref": "#/components/schemas/CampusDataset"
              }
            }
          }
        ]
      },
      "CampusMessageSummary": {
        "type": "object",
        "required": [
          "id",
          "title",
          "publisher",
          "publishedAt"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "title": {
            "type": "string"
          },
          "publisher": {
            "type": "string"
          },
          "publishedAt": {
            "type": "string"
          }
        },
        "additionalProperties": false
      },
      "CampusMessageLink": {
        "type": "object",
        "required": [
          "label",
          "url"
        ],
        "properties": {
          "label": {
            "type": "string"
          },
          "url": {
            "type": "string",
            "format": "uri"
          }
        },
        "additionalProperties": false
      },
      "CampusMessageDetail": {
        "type": "object",
        "required": [
          "id",
          "title",
          "publisher",
          "publishedAt",
          "content",
          "links"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "title": {
            "type": "string"
          },
          "publisher": {
            "type": "string"
          },
          "publishedAt": {
            "type": "string"
          },
          "content": {
            "type": "string",
            "description": "Plain text; never execute this as HTML."
          },
          "links": {
            "type": "array",
            "items": {
              "$ref": "#/components/schemas/CampusMessageLink"
            }
          }
        },
        "additionalProperties": false
      },
      "CampusMessageDetailResponse": {
        "allOf": [
          {
            "$ref": "../components/schemas.yaml#/ApiSuccess"
          },
          {
            "type": "object",
            "required": [
              "result"
            ],
            "properties": {
              "result": {
                "$ref": "#/components/schemas/CampusMessageDetail"
              }
            }
          }
        ]
      }
    }
  },
  "message": {
    "get": {
      "tags": [
        "Campus"
      ],
      "summary": "Read one campus message",
      "operationId": "campusMessage",
      "security": [
        {
          "bearerAuth": []
        },
        {
          "accessTokenCookie": []
        }
      ],
      "description": "Private plain-text message body and explicit links. Requires the message to appear in the current user list. No automatic remote media loads. A 409 campus.messageAuthorizationRequired response requests updated school authorization, preserving the current binding.",
      "responses": {
        "200": {
          "description": "Successful private response; Cache-Control: private, no-store.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CampusMessageDetailResponse"
              },
              "examples": {
                "success": {
                  "externalValue": "../fixtures/campus-message-success.json"
                }
              }
            }
          }
        },
        "default": {
          "description": "Session, CSRF, writable-account, expired authorization, identity conflict or upstream failure.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiFailure"
              }
            }
          }
        },
        "401": {
          "description": "Missing, invalid or revoked forum session.",
          "content": {
            "application/json": {
              "schema": {
                "$ref": "../components/schemas.yaml#/ApiFailure"
              }
            }
          }
        }
      },
      "parameters": [
        {
          "in": "path",
          "name": "messageId",
          "required": true,
          "schema": {
            "type": "string",
            "pattern": "^[1-9][0-9]{0,19}$",
            "maxLength": 20
          }
        }
      ]
    }
  }
}
